name: "Publish"
description: "Build and publish the image"
inputs:
  registry_fqdn:
    description: "what is the fqdn of the registry used"
    required: true
    default: ghcr.io
  registry_repo:
    description: "what is the repository of the registry used"
    required: true
    default: ${{ github.repository }}
  registry_username:
    description: "what is the username used to login to the registry"
    required: true
  registry_password:
    description: "what is the password used to login to the registry"
    required: true
  image_tag:
    description: "what is the tag of the image to push"
    required: true
runs:
  using: "composite"
  steps:
    - name: Login to Github Container Registry
      run: docker login -u ${{ inputs.registry_username }} -p ${{ inputs.registry_password }} ${{ inputs.registry_fqdn }}/${{ inputs.registry_repo }}
      shell: bash
    - name: Push image
      run: docker push ${{ inputs.registry_fqdn }}/${{ inputs.registry_repo }}/${{ inputs.image_tag }}
      shell: bash
